global:
  scrape_interval: {{ prometheus_scrape_interval }}

scrape_configs:
{% for target in prometheus_scrape_targets %}
  - job_name: {{ target.job_name }}
    static_configs:
      - targets:
{% for addr in target.targets %}
          - "{{ addr }}"
{% endfor %}
{% endfor %}
